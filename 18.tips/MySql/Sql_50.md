以下是leetcode高频SQL基础50题的技巧总结、答案以及SQL语言提示，按题号分类，且SQL语言提示列位于答案列之前：

| 题号 | 题目名称                       | 技巧总结                                                     | 答案                                                         |
| ---- | ------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| 1757 | 可回收且低脂的产品             | 使用WHERE子句筛选满足多个条件的记录                          | SELECT product_id FROM Products WHERE is_recyclable = 1 AND is_low_fat = 1 |
| 584  | 寻找用户推荐人                 | 使用LEFT JOIN连接用户表和推荐人表，筛选推荐人非空且推荐人id为2的记录 | SELECT name FROM Employee WHERE manager_id = 2               |
| 595  | 大的国家                       | 使用WHERE子句筛选面积大于3百万或人口大于25百万的国家         | SELECT name FROM Countries WHERE area > 3000000 OR population > 25000000 |
| 1148 | 文章浏览 I                     | 使用COUNT函数统计浏览次数，并使用WHERE子句筛选浏览次数大于1的记录 | SELECT article_id, COUNT(*) AS num FROM Views GROUP BY article_id HAVING num > 1 |
| 1683 | 无效的推文                     | 使用WHERE子句筛选文本为空的推文                              | SELECT tweet_id FROM Tweets WHERE content IS NULL            |
| 1378 | 使用唯一标识码替换员工ID       | 使用CASE WHEN语句将空的唯一标识码替换为姓名首字母            | SELECT employee_id, CASE WHEN unique_id IS NULL THEN SUBSTRING(name, 1, 1) ELSE unique_id END AS unique_id FROM Employees |
| 1068 | 产品销售分析 I                 | 使用WHERE子句筛选销售月份为2018年12月的产品                  | SELECT product_id FROM Sales WHERE sale_date BETWEEN '2018-12-01' AND '2018-12-31' |
| 1581 | 进店却未进行过交易的顾客       | 使用LEFT JOIN连接顾客表和交易表，筛选交易表中金额为空的顾客  | SELECT c.customer_id FROM Customers c LEFT JOIN Transactions t ON c.customer_id = t.customer_id WHERE t.amount IS NULL |
| 197  | 上升的温度                     | 使用自连接比较同一城市不同日期的温度，筛选出温度上升的记录   | SELECT w1.id FROM Weather w1 JOIN Weather w2 ON w1.recordDate = w2.recordDate + INTERVAL 1 DAY AND w1.temperature > w2.temperature |
| 1661 | 每台机器的进程平均运行时间     | 使用CASE WHEN语句计算运行时间，再使用GROUP BY和ROUND函数计算平均运行时间 | SELECT machine_id, ROUND(AVG(CASE WHEN process_id % 2 = 0 THEN timestamp ELSE NULL END - CASE WHEN process_id % 2 = 1 THEN timestamp ELSE NULL END), 3) AS processing_time FROM Activity GROUP BY machine_id |
| 577  | 员工奖金                       | 使用LEFT JOIN连接员工表和奖金表，筛选奖金小于1000或无奖金的员工 | SELECT e.name, b.bonus FROM Employee e LEFT JOIN Bonus b ON e.empId = b.empId WHERE b.bonus < 1000 OR b.bonus IS NULL |
| 1280 | 学生们参加各科测试的次数       | 使用CROSS JOIN生成学生和科目的笛卡尔积，再通过LEFT JOIN和COUNT函数统计参加次数 | SELECT s.student_id, s.student_name, sub.subject_name, COUNT(e.subject_name) AS attended_exams FROM Students s CROSS JOIN Subjects sub LEFT JOIN Examinations e ON s.student_id = e.student_id AND sub.subject_name = e.subject_name GROUP BY s.student_id, sub.subject_name |
| 570  | 至少有5名直接下属的经理        | 使用自连接统计每个经理的直接下属数量，筛选出数量大于等于5的经理 | SELECT manager_id FROM Employee WHERE employee_id IN (SELECT manager_id FROM Employee GROUP BY manager_id HAVING COUNT(*) >= 5) |
| 1934 | 确认率                         | 使用LEFT JOIN连接注册表和确认表，使用SUM和COUNT函数计算确认率 | SELECT u.user_id, ROUND(SUM(CASE WHEN c.action = 'confirmed' THEN 1 ELSE 0 END) / COUNT(*), 2) AS confirmation_rate FROM Users u LEFT JOIN Confirmations c ON u.user_id = c.user_id GROUP BY u.user_id |
| 620  | 有趣的电影                     | 使用WHERE子句筛选描述不为“boring”且id为奇数的电影，并按评分降序排列 | SELECT * FROM cinema WHERE description <> 'boring' AND id % 2 = 1 ORDER BY rating DESC |
| 1251 | 平均售价                       | 使用LEFT JOIN连接价格表和销售表，通过SUM和COUNT函数计算平均售价 | SELECT p.product_id, ROUND(SUM(p.price * u.units) / SUM(u.units), 2) AS average_price FROM Prices p LEFT JOIN UnitsSold u ON p.product_id = u.product_id AND u.purchase_date BETWEEN p.start_date AND p.end_date GROUP BY p.product_id |
| 1075 | 项目员工 I                     | 使用LEFT JOIN连接项目表和员工表，计算每个项目的平均经验年数  | SELECT p.project_id, ROUND(AVG(e.experience_years), 2) AS average_years FROM Project p LEFT JOIN Employee e ON p.employee_id = e.employee_id GROUP BY p.project_id |
| 1633 | 各赛事的用户注册率             | 使用LEFT JOIN连接注册表和用户表，计算每个赛事的注册率        | SELECT c.contest_id, ROUND(COUNT(r.user_id) / (SELECT COUNT(*) FROM Users) * 100, 2) AS percentage FROM Contests c LEFT JOIN Register r ON c.contest_id = r.contest_id GROUP BY c.contest_id ORDER BY percentage DESC, c.contest_id |
| 1211 | 查询结果的质量和占比           | 使用GROUP BY分组，计算每个查询的质量和差评率                 | SELECT query_name, ROUND(AVG(rating), 2) AS quality, ROUND(SUM(CASE WHEN rating < 3 THEN 1 ELSE 0 END) / COUNT(*) * 100, 2) AS poor_query_percentage FROM Queries GROUP BY query_name |
| 1193 | 每月交易 I                     | 使用GROUP BY按月和国家分组，统计交易数量、金额等信息         | SELECT DATE_FORMAT(trans_date, '%Y-%m') AS month, country, COUNT(*) AS trans_count, SUM(CASE WHEN state = 'approved' THEN 1 ELSE 0 END) AS approved_count, SUM(amount) AS trans_total_amount, SUM(CASE WHEN state = 'approved' THEN amount ELSE 0 END) AS approved_total_amount FROM Transactions GROUP BY month, country |
| 1174 | 即时食物配送 II                | 使用子查询筛选出每个用户的首单，再计算即时配送的百分比       | SELECT ROUND(SUM(CASE WHEN order_date = customer_pref_delivery_date THEN 1 ELSE 0 END) / COUNT(*) * 100, 2) AS immediate_percentage FROM (SELECT *, ROW_NUMBER() OVER (PARTITION BY customer_id ORDER BY order_date) AS rn FROM Delivery) sub WHERE rn = 1 |
| 550  | 游戏玩法分析 IV                | 使用子查询和自连接，计算连续两天登录的玩家比例               | SELECT ROUND(COUNT(DISTINCT a.player_id) / (SELECT COUNT(DISTINCT player_id) FROM Activity) * 100, 2) AS percentage FROM Activity a JOIN Activity b ON a.player_id = b.player_id AND a.event_date = b.event_date + INTERVAL 1 DAY |
| 2356 | 每位教师所教授的科目种类的数量 | 使用GROUP BY和COUNT函数统计每位教师教授的科目种类数          | SELECT teacher_id, COUNT(DISTINCT subject_id) AS cnt FROM Teacher GROUP BY teacher_id |
| 1141 | 查询近30天活跃用户数           | 使用GROUP BY按日期分组，筛选出近30天的活跃用户数             | SELECT active_date, COUNT(DISTINCT user_id) AS active_users FROM Logins WHERE active_date BETWEEN DATE_SUB('2019-06-30', INTERVAL 29 DAY) AND '2019-06-30' GROUP BY active_date |
| 1084 | 销售分析III                    | 使用GROUP BY分组，通过SUM函数判断所有销售是否在指定日期范围内 | SELECT product_id FROM Sales GROUP BY product_id HAVING SUM(CASE WHEN sale_date < '2019-01-01' OR sale_date > '2019-03-01' THEN 1 ELSE 0 END) = 0 |
| 596  | 超过5名学生的课                | 使用GROUP BY分组，筛选出学生数量大于等于5的课程              | SELECT class FROM Courses GROUP BY class HAVING COUNT(DISTINCT student) >= 5 |
| 1729 | 求关注者的数量                 | 使用GROUP BY分组，统计每个用户的关注者数量                   | SELECT user_id, COUNT(follower_id) AS followers_count FROM Followers GROUP BY user_id |
| 619  | 只出现一次的最大数字           | 使用GROUP BY分组，筛选出只出现一次的数字，再取最大值         | SELECT MAX(num) AS num FROM (SELECT num FROM MyNumbers GROUP BY num HAVING COUNT(*) = 1) sub |
| 1045 | 买下所有产品的客户             | 使用GROUP BY分组，筛选出购买产品种类等于总产品种类的客户     | SELECT customer_id FROM Orders GROUP BY customer_id HAVING COUNT(DISTINCT product_id) = (SELECT COUNT(DISTINCT product_id) FROM Products) |
| 1731 | 每位经理的下属员工数量         | 使用自连接统计每个经理的下属数量和平均年龄                   | SELECT manager_id, COUNT(*) AS direct_reports, AVG(age) AS average_age FROM Employee WHERE manager_id IN (SELECT DISTINCT manager_id FROM Employee) GROUP BY manager_id |
| 1789 | 员工的直属部门                 | 使用UNION合并两个查询结果，一个是primary_flag为'Y'的记录，另一个是只有一个部门的员工 | SELECT employee_id, department_id FROM Employee WHERE primary_flag = 'Y' UNION SELECT employee_id, department_id FROM Employee GROUP BY employee_id HAVING COUNT(*) = 1 |
| 610  | 判断三角形                     | 使用CASE WHEN语句判断三条边是否能构成三角形                  | SELECT x, y, z, CASE WHEN x + y > z AND x + z > y AND y + z > x THEN 'Yes' ELSE 'No' END AS triangle FROM Triangle |
| 180  | 连续出现的数字                 | 使用自连接比较连续记录的数值是否相同，筛选出连续出现的数字   | SELECT DISTINCT a.num AS ConsecutiveNums FROM Logs a JOIN Logs b ON a.id = b.id + 1 AND a.num = b.num JOIN Logs c ON a.id = c.id + 2 AND a.num = c.num |
| 1164 | 指定日期的产品价格             | 使用子查询和UNION，找出指定日期前的最新价格，若无则默认为10  | SELECT product_id, 10 AS price FROM Products WHERE product_id NOT IN (SELECT product_id FROM Prices WHERE start_date <= '2019-08-16' AND end_date >= '2019-08-16') UNION SELECT product_id, price FROM Prices WHERE (product_id, start_date) IN (SELECT product_id, MAX(start_date) FROM Prices WHERE start_date <= '2019-08-16' GROUP BY product_id) |
| 1204 | 最后一个能进入巴士的人         | 使用自连接和GROUP BY，筛选出总重量不超过1000的最后一个人     | SELECT person_name FROM Queue WHERE turn = (SELECT MAX(turn) FROM (SELECT turn, SUM(weight) OVER (ORDER BY turn) AS total_weight FROM Queue) sub WHERE total_weight <= 1000) |
| 1907 | 按分类统计薪水                 | 使用UNION合并三个查询结果，分别统计低、中、高薪水的账户数量  | SELECT 'Low Salary' AS category, COUNT(*) AS accounts_count FROM Accounts WHERE income < 20000 UNION SELECT 'Average Salary' AS category, COUNT(*) AS accounts_count FROM Accounts WHERE income BETWEEN 20000 AND 50000 UNION SELECT 'High Salary' AS category, COUNT(*) AS accounts_count FROM Accounts WHERE income > 50000 |
| 1978 | 上级经理已离职的公司员工       | 使用NOT IN筛选出经理id不在员工表中的员工                     | SELECT employee_id FROM Employees WHERE manager_id NOT IN (SELECT employee_id FROM Employees) |
| 626  | 换座位                         | 使用CASE WHEN语句根据id的奇偶性调整座位                      |                                                              |